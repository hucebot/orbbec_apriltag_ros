from launch import LaunchDescription
from launch.actions import DeclareLaunchArgument
from launch.conditions import IfCondition
from launch.substitutions import LaunchConfiguration, PythonExpression
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription([
        DeclareLaunchArgument('verbose', default_value='false'),
        DeclareLaunchArgument('display', default_value='false'),
        DeclareLaunchArgument('image_topic', default_value='/camera/camera/color/image_raw',
            description='RGB image topic'),
        DeclareLaunchArgument('depth_topic', default_value='/camera/camera/aligned_depth_to_color/image_raw',
            description='Depth image topic (aligned to color)'),
        DeclareLaunchArgument('camera_info_topic', default_value='/camera/camera/aligned_depth_to_color/camera_info',
            description='CameraInfo topic for intrinsics'),
        DeclareLaunchArgument('depth_scale', default_value='0.001',
            description='Depth pixel value to meters scale factor'),
        DeclareLaunchArgument('publish_tf', default_value='false'),
        DeclareLaunchArgument('tag_frame_prefix', default_value='apriltag',
            description='Prefix for per-tag TF child frames (e.g. apriltag_0, apriltag_1)'),
        DeclareLaunchArgument('publishing_frame', default_value='',
            description='If set, transform poses from camera frame to this frame before publishing. If empty, publish in camera frame.'),
        DeclareLaunchArgument('transform_timeout', default_value='0.1',
            description='Timeout in seconds for TF lookup when publishing_frame is set'),
        DeclareLaunchArgument('min_decision_margin', default_value='0.0',
            description='Minimum decision margin for AprilTag detection filtering (0.0 = no filtering)'),
        DeclareLaunchArgument('fix_normal_axis', default_value='',
            description='Fix tag normal to parent frame axis: "x","-x","y","-y","z","-z" or "" (disabled)'),
        DeclareLaunchArgument('debug', default_value='false',
            description='Enable debug publishers (point cloud + segmentation image of detected tags)'),
        DeclareLaunchArgument('enable_apriltag_to_box', default_value='false',
            description='Launch apriltag_to_box node to convert tag pose to box obstacle'),
        DeclareLaunchArgument('apriltag_to_box_tag_id', default_value='23',
            description='AprilTag ID to use for box obstacle'),
        DeclareLaunchArgument('box_pose_topic', default_value='/g1pilot/box_pose',
            description='Output topic for box obstacle marker'),
        DeclareLaunchArgument('box_publishing_frame', default_value='',
            description='Frame to transform box marker into (empty = keep input frame)'),
        DeclareLaunchArgument('box_extents', default_value='0.02,0.0,1.09,0.30,0.25,0.56',
            description='Box extents: x_minus,x_plus,y_minus,y_plus,z_minus,z_plus (meters)'),
        Node(
            package='apriltag_pose',
            executable='apriltag_pose',
            name='apriltag_pose',
            output='screen',
            parameters=[{
                'verbose': LaunchConfiguration('verbose'),
                'display': LaunchConfiguration('display'),
                'input_mode': 'rgbd',
                'image_topic': LaunchConfiguration('image_topic'),
                'depth_topic': LaunchConfiguration('depth_topic'),
                'camera_info_topic': LaunchConfiguration('camera_info_topic'),
                'depth_scale': LaunchConfiguration('depth_scale'),
                'publish_tf': LaunchConfiguration('publish_tf'),
                'tag_frame_prefix': LaunchConfiguration('tag_frame_prefix'),
                'publishing_frame': LaunchConfiguration('publishing_frame'),
                'transform_timeout': LaunchConfiguration('transform_timeout'),
                'min_decision_margin': LaunchConfiguration('min_decision_margin'),
                'fix_normal_axis': LaunchConfiguration('fix_normal_axis'),
                'debug': LaunchConfiguration('debug'),
            }],
        ),
        Node(
            condition=IfCondition(LaunchConfiguration('enable_apriltag_to_box')),
            package='apriltag_pose',
            executable='apriltag_to_box.py',
            name='apriltag_to_box',
            output='screen',
            parameters=[{
                'apriltag_pose_topic': PythonExpression([
                    "'apriltag_pose/pose_tag_' + '",
                    LaunchConfiguration('apriltag_to_box_tag_id'),
                    "'",
                ]),
                'box_topic': LaunchConfiguration('box_pose_topic'),
                'publishing_frame': LaunchConfiguration('box_publishing_frame'),
                'box_extents': LaunchConfiguration('box_extents'),
            }],
        ),
    ])
